root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/crawler ./cmd/crawler"
bin = "./tmp/crawler"
include_ext = ["go"]
exclude_dir = ["tmp"]
delay = 200
poll = true
poll_interval = 500

[log]
time = true
